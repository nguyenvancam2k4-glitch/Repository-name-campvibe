# Step 30.1 - ScrollToTop Fix

Đã thêm component `ScrollToTop` trong `App.jsx`.

Mục đích:
- Khi chuyển route, ví dụ từ `/places` sang `/places/1`, trang sẽ tự cuộn lên đầu.
- Sửa lỗi người dùng bấm xem chi tiết nhưng trang mới lại mở ở đoạn dưới.
- Áp dụng cho toàn bộ route: places, booking, blog, contact, account, favorites, compare, admin, 404.
